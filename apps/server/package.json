{
	"name": "server",
	"private": true,
	"version": "0.0.0",
	"scripts": {
        "build": "bun build index.ts --outdir ./out --target node",
		"dev": "bun --watch run index.ts",
		"start": "bun run index.ts"
	},
	"devDependencies": {
		"@types/node": "^20.6.1",
		"dotenv": "^16.3.1",
		"eslint": "^8.49.0",
		"eslint-config-custom-server": "*",
		"tsconfig": "*",
		"typescript": "^5.0.2"
	},
	"dependencies": {
		"socket.io": "^4.7.2",
		"socket.io-client": "^4.7.2"
	}
}
